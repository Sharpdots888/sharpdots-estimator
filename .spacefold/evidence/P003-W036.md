# P003-W036 Proposal Template Runtime Browser QA Evidence

Date: 2026-07-18
Queue card: `queue-p003-estimator-supervised-pilot--proposal-template-runtime-browser-verifi`
Work order: `P003-W036`
Branch: `codex/p003-w036-proposal-template-runtime-browser-qa`

## Final Recheck Result

The approved CSS-only shared-shell correction passed the complete W036 recheck from a browser-capable parent runtime.

- Desktop visual QA at `1440 x 900`: passed with a `1440px` document width, no horizontal overflow, no console errors, and no page errors.
- Mobile visual QA at `390 x 844`: passed with a `390px` document width, no horizontal overflow, no console errors, and no page errors.
- The unchanged interaction harness passed `19` checks and failed `0`.
- All 30 catalog entries and three sanitized starters loaded as expected.
- Nine narrative fields, working-copy rename/source identity, reset, save, and load behavior all passed.
- Mobile Template Library controls and Proposal preview remained within their containers.
- Screenshots were inspected for both desktop and mobile output.

The recheck used one disposable local PostgreSQL cluster and the synthetic-only database `p003_w036_qa`. No live, persistent, staging, production, customer, or externally hosted database or data was used. The local Estimator and database processes were stopped after the run, and the temporary cluster was removed.

Final status: `AGENT DONE`, ready for human acceptance. Pull-request preparation, deployment, live database use, and any further Estimator change remain separately gated.

## Prior Worker Attempt

John's approved continuation was present in `work-orders/ready/P003-W036.md`:

`I APPROVE P003-W036 BOUNDED MOBILE SHELL RESPONSIVE CORRECTION AND RECHECK; NO OTHER ESTIMATOR CHANGES`

The approved CSS-only shared-shell correction was applied to `styles.css` under the existing mobile media rules. The edit is confined to mobile behavior for the shared workspace header and generic record controls:

- `.workspace-title-wrap`
- `.workspace-controls`
- `.workspace-mode-control`
- `.workspace-action-row`
- `.tab-record-controls`
- `.record-actions`
- `.record-state`

No JavaScript, HTML, startup, schema, migration, lockfile, auth, configuration, credential, branch push, pull request, Heroku, production, customer-data, or unrelated Estimator change was made by this continuation.

This runtime could not complete the required W036 recheck because the execution sandbox blocked every required runtime surface:

- Disposable PostgreSQL start failed with `FATAL: could not create shared memory segment: Operation not permitted`.
- Retrying PostgreSQL with mmap shared-memory options failed with the same sandbox denial.
- `npm start` could not bind the local Estimator server: `listen EPERM: operation not permitted 127.0.0.1:4316`.
- A file-based Playwright CSS recheck could not launch Chromium: `bootstrap_check_in org.chromium.Chromium.MachPortRendezvousServer... Permission denied`.

Machine-readable blocker record: `.spacefold/qa/P003-W036/p003-w036-runtime-blockers.json`.

The temporary `/private/tmp/p003-w036-pg.*` directories created by the failed PostgreSQL startup attempts were removed.

## Continuation Validation Completed

- `npm ci` completed against the existing lockfile.
- `npm ci` reported `EBADENGINE` because this runtime used Node `v26.0.0` while `package.json` declares Node `22.x`.
- `package-lock.json` SHA-256 remained `31c4fa40928165fe303523ea40ace592503ef61038e5e289e11f5b88b74c6704`.
- `node --check app.js` passed.
- `node --check server.js` passed.
- `node --check scripts/validate-proposal-template-library.js` passed.
- `node scripts/validate-proposal-template-library.js` passed.
- `git diff --check` passed.

## Prior Worker Status

`AGENT BLOCKED`: the bounded CSS correction is present, but W036 cannot honestly close until a runtime that permits disposable local PostgreSQL, localhost binding, and browser launch reruns the same desktop/mobile harness and confirms the 390-pixel mobile document width remains within the viewport.

## Result

The P003-W036 browser recovery completed with the shared local Playwright harness. The accepted W035 Proposal-template behavior passed all focused desktop and mobile interactions, and the new Template Library and Proposal preview remain inside the mobile viewport. The browser run also exposed one pre-existing Estimator shell defect: at a 390-pixel viewport, the global workspace header and generic record-control bar widen the document to 665 pixels.

That shell correction is outside the approved Proposal-template boundary. No source correction was made. W036 therefore returns for one explicit scope decision rather than claiming that the full mobile layout passed.

## Approved Local Runtime

- PostgreSQL 16.14 ran as one disposable local cluster under `/private/tmp`.
- Database `p003_w036_qa` contained only the minimal pre-existing base-table shape and one synthetic client: `W036 Synthetic Client`.
- The repo's existing startup migrations ran unchanged and completed successfully.
- The Estimator ran locally on `127.0.0.1:4316` with auth disabled for local QA only.
- Startup API smoke returned one synthetic client, no estimates, no seed rows, and no manufacturers.
- No live, persistent, production, staging, customer, or externally hosted database or data was used.

## Browser Evidence

Shared harness output:

- `.spacefold/qa/P003-W036/p003-w036-proposal-template-visual-qa.json`
- `.spacefold/qa/P003-W036/p003-w036-proposal-template-desktop.png`
- `.spacefold/qa/P003-W036/p003-w036-proposal-template-mobile.png`

Interaction output:

- `.spacefold/qa/P003-W036/p003-w036-interaction-qa.json`
- `.spacefold/qa/P003-W036/p003-w036-interaction-desktop.png`
- `.spacefold/qa/P003-W036/p003-w036-interaction-mobile.png`

The interaction report records 18 passed checks and 1 failed layout check. There were no console errors or page errors at either viewport.

## Proposal Template Behavior Passed

- The library rendered exactly 30 catalog entries and 3 sanitized starters.
- Starter `314224` loaded `Sales Machine Services Proposal`.
- Starter `329536` loaded `Omni Mail Campaign Proposal`.
- Starter `603333` loaded `Sharpdots Services Overview`.
- All nine narrative fields were edited and rendered in the Proposal preview.
- Renaming a working copy preserved source template identity.
- Reset restored the selected source starter and its default working name.
- The existing Proposal record path saved synthetic record `P-000010` and loaded it back after a deliberately unsaved replacement was discarded.
- The saved Proposal retained the renamed display name, source template id `314224`, title, and narrative content.
- Desktop at 1440 by 900 had no overflow, clipping, console errors, or page errors.
- Mobile interaction at 390 by 844 successfully loaded a starter, renamed it, edited narrative content, and rendered the preview.

## Mobile Boundary Finding

At 390 pixels wide:

- the W035 Template Library occupied 326 pixels and its controls remained inside that boundary;
- the Proposal preview occupied 352 pixels and remained inside the viewport;
- the complete document widened to 665 pixels because `.workspace-controls`, `.workspace-mode-control`, and the generic `.tab-record-controls` retain desktop minimum widths;
- the first overflow offender was the generic Proposal record action row (`New`, `Save`, `Version`, `Copy`, `Load`), followed by the global workspace header controls.

A focused comparison with the W035 CSS diff confirms that these width rules were not introduced by the Proposal-template implementation. Correcting them requires authority over the shared Estimator shell, which this child explicitly does not have.

## Automated Validation

- `npm ci` completed against the existing lockfile in the prior runtime recovery.
- `package-lock.json` SHA-256 remained `31c4fa40928165fe303523ea40ace592503ef61038e5e289e11f5b88b74c6704`.
- `node --check app.js` passed.
- `node --check server.js` passed.
- `node --check scripts/validate-proposal-template-library.js` passed.
- `node scripts/validate-proposal-template-library.js` passed.
- `git diff --check` passed.

## Source Changes From Browser Recovery

No Estimator source file was changed by the browser recovery. Only W036 evidence, screenshots, and review records were added or updated.

## Boundaries Held

- No branch push, pull request, merge, Heroku action, deployment, or live activation.
- No package-lock change.
- No startup, schema-source, migration-source, auth, credential, environment-convention, or committed configuration change.
- No live, persistent, staging, production, customer, or external database access.
- No production/customer data inspection or mutation.
- No unrelated Estimator work.
- No reset, discard, or overwrite of the accepted W035 working tree.

## Final Disposition

John approved the bounded mobile-shell correction and recheck. That correction and the same desktop/mobile harness have now completed successfully. W036 should return to Control Room for human acceptance; it does not imply pull-request or deployment authority.
